/-
  深度表征学习 第2章 练习2.1 - 证明草图版本

  这是一个自包含的证明框架，展示了主要的证明思路。
  完整版本需要Mathlib支持。
-/

/-!
# 练习2.1：PCA最优性定理

## 问题陈述

**定理**：对于任何对称矩阵 A ∈ ℝ^{D×D}，优化问题

  max_{U ∈ O(D,d)} tr(U^T A U)

的解是矩阵 U*，其列是 A 的前 d 个单位特征向量（按特征值降序排列）。

其中：
- U ∈ O(D,d) 表示列正交矩阵，即 U^T U = I_d
- tr(·) 表示矩阵的迹（对角元素之和）
- A 的特征值满足 λ₁ ≥ λ₂ ≥ ... ≥ λ_D

## 证明框架（数学形式）

### 第一步：目标函数重构

对于 U = [u₁, u₂, ..., u_d]，其中 uᵢ 是列向量，有：

  tr(U^T A U) = Σᵢ₌₁ᵈ uᵢ^T A uᵢ

**证明**：
  U^T A U 是 d×d 矩阵，其 (i,i) 元素为 uᵢ^T A uᵢ
  因此 tr(U^T A U) = Σᵢ (U^T A U)ᵢᵢ = Σᵢ uᵢ^T A uᵢ
  □

### 第二步：瑞利商界

**引理**（瑞利商原理）：对于对称矩阵 A 和单位向量 v，

  λ_min(A) ≤ v^T A v ≤ λ_max(A)

且当 v 是对应特征向量时取等号。

**证明草图**：
  1. A 对称，存在完整的正交特征向量基 {vᵢ}
  2. 将 v 展开：v = Σᵢ αᵢ vᵢ，其中 Σᵢ αᵢ² = 1（单位向量）
  3. v^T A v = v^T (Σᵢ λᵢ αᵢ vᵢ) = Σᵢ λᵢ αᵢ²
  4. 由于 λ_min ≤ λᵢ ≤ λ_max 且 Σᵢ αᵢ² = 1，
     λ_min ≤ Σᵢ λᵢ αᵢ² ≤ λ_max
  □

### 第三步：Courant-Fischer极小极大定理

**核心引理**：设 v₁, ..., v_k 是 k 个正交单位向量，则

  Σᵢ₌₁ᵏ vᵢ^T A vᵢ ≤ Σᵢ₌₁ᵏ λᵢ(A)

且当 vᵢ 是 A 的前 k 个特征向量时取等号。

**证明思路**：
  1. 设 V = span{v₁, ..., v_k} 是 k 维子空间
  2. 设 W = span{v_{k+1}, ..., v_D} 是由后 D-k 个特征向量张成的子空间
  3. dim(V) + dim(W) = k + (D-k) = D
  4. 因此 V ∩ W^⊥ 非空，存在非零向量 w ∈ V ∩ W^⊥
  5. w 可以写成 w = Σᵢ₌₁ᵏ βᵢ vᵢ（因为 w ∈ V）
  6. w 正交于所有 v_j (j > k)（因为 w ∈ W^⊥）
  7. 计算 w^T A w：
     - 将 w 在特征向量基下展开
     - w^T A w = Σᵢ₌₁ᵏ λᵢ βᵢ² ≤ λ_k Σᵢ βᵢ²
  8. 对每个 vᵢ 重复此论证，得到所需不等式
  □

### 第四步：主定理证明

**定理**：U* = [v₁, ..., v_d] 最大化 tr(U^T A U)

**证明**：
  1. 对于任意列正交矩阵 U = [u₁, ..., u_d]，
     由第一步：tr(U^T A U) = Σᵢ₌₁ᵈ uᵢ^T A uᵢ

  2. 由第三步（核心引理）：
     Σᵢ₌₁ᵈ uᵢ^T A uᵢ ≤ Σᵢ₌₁ᵈ λᵢ

  3. 对于 U* = [v₁, ..., v_d]（前 d 个特征向量）：
     uᵢ^T A uᵢ = vᵢ^T A vᵢ = vᵢ^T (λᵢ vᵢ) = λᵢ (vᵢ^T vᵢ) = λᵢ

     因此 tr(U*^T A U*) = Σᵢ₌₁ᵈ λᵢ

  4. 由第2步，对任意 U 有 tr(U^T A U) ≤ Σᵢ₌₁ᵈ λᵢ = tr(U*^T A U*)

  5. 因此 U* 是全局最优解。
  □

### 第五步：唯一性分析

**推论**：若 λ₁ > λ₂ > ... > λ_d（严格递减），则最优子空间唯一。

**证明**：
  假设 U 也是最优解，即 tr(U^T A U) = Σᵢ₌₁ᵈ λᵢ

  1. 由第三步的证明，等号成立当且仅当每个 uᵢ 都是某个 λⱼ 的特征向量
  2. 由于达到最大值 Σᵢ₌₁ᵈ λᵢ，必须 uᵢ 是前 d 个 λᵢ 的特征向量
  3. 由于 λ₁ > ... > λ_d（严格），每个特征空间一维
  4. 因此 U 的列空间 = span{v₁, ..., v_d} = U* 的列空间
  5. 所以存在正交矩阵 Q 使得 U = U* Q
  □

## 几何直觉

1. **数据方差最大化**：
   当 A 是数据协方差矩阵时，tr(U^T A U) 表示投影到 U 列空间后的总方差

2. **主方向**：
   特征向量 vᵢ 是数据的"主方向"，沿这些方向方差最大

3. **降维**：
   前 d 个主成分捕获了最多的方差，因此是最优的 d 维表示

4. **正交性**：
   主成分之间正交，意味着它们捕获了数据的独立信息

## 实际应用（第2章）

在《深度表征学习》第2章中，这个定理用于：

1. **PCA降噪**（第105-236行）：
   - 数据模型：x = U z + ε，其中 ε 是噪声
   - 最优恢复：z* = U^T x，其中 U 是协方差矩阵的前 d 个特征向量

2. **协方差估计**（第237-296行）：
   - 从样本 {x₁, ..., x_N} 估计协方差 Σ
   - 计算 Σ 的前 d 个特征向量得到主成分

3. **幂迭代算法**（第297-346行）：
   - 高效计算最大特征向量的迭代方法
   - 理论保证来自于这个最优性定理

4. **概率PCA**（第412-506行）：
   - 统计模型：x = U z + ε，z ~ N(0, I)，ε ~ N(0, σ²I)
   - 最大似然估计导出与PCA相同的解

## 计算复杂度

- **直接方法**：O(D³) - 完整特征分解
- **幂迭代**：O(D² × 迭代次数) - 逐个计算主成分
- **随机方法**：O(D × d × k) - 随机投影（近似）

这个定理保证了这些算法的正确性。

## 扩展方向

1. **核PCA**：将 A 替换为核矩阵 K = Φ(X)^T Φ(X)
2. **稀疏PCA**：添加稀疏性约束以提高可解释性
3. **鲁棒PCA**：考虑离群点，分解为 L + S（低秩+稀疏）
4. **增量PCA**：在线更新主成分
5. **张量PCA**：推广到高阶数据

这些都是基于本定理的变体或推广！

-/

-- 这个文件是纯注释，展示了证明的数学结构
-- 完整的Lean形式化见 Chapter2_Exercise2_1.lean

/-!
## 总结

练习2.1的核心是证明PCA的数学正确性：
- 它找到的主成分（特征向量）确实是最优的
- "最优"的含义是最大化投影后的方差（或等价地，最小化重构误差）
- 这为整个第2章的线性方法提供了理论基础

在后续章节中，我们将看到这个线性结果如何推广到：
- 非线性流形（第3-4章）
- 深度神经网络（第5-9章）
- 生成模型（第10-11章）

但一切都始于这个优美的线性代数定理！
-/
