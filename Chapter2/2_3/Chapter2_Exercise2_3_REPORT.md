Exercise 2.3 当前状态

  编译状态：⚠️ 有 sorry（未完全证明）

  最新更新：2026-03-26

  已完成的证明：
  - IsDiagonal、Covariance、IsUncorrelated 定义
  - diagonal_transpose 引理
  - ✅ diagonal_mul 引理（行104-115）- 2026-03-26 完成
  - hD1_diag、hD2_diag 证明
  - ✅ GL_symmetry 定理（行63-72）
  - ✅ hA_D1_Q_D2 恒等式（行172-182）

  剩余的 sorry（7个）：

  ┌─────────┬──────────────────────────┬──────┐
  │  行号   │           内容           │ 难度 │
  ├─────────┼──────────────────────────┼──────┤
  │ 133     │ h_AAt_diag 关键引理     │ 高   │
  ├─────────┼──────────────────────────┼──────┤
  │ 137     │ h_AtA_diag              │ 高   │
  ├─────────┼──────────────────────────┼──────┤
  │ 150     │ h_D1_pos 内部sorry      │ 中   │
  ├─────────┼──────────────────────────┼──────┤
  │ 155     │ h_D2_pos 内部sorry      │ 中   │
  ├─────────┼──────────────────────────┼──────┤
  │ 163     │ hD1_inv (可逆性)        │ 中   │
  ├─────────┼──────────────────────────┼──────┤
  │ 164     │ hD2_inv (可逆性)        │ 中   │
  ├─────────┼──────────────────────────┼──────┤
  │ 185     │ hQ_orth (正交性)        │ 高   │
  └─────────┴──────────────────────────┴──────┘

  进度统计：
  - 总证明目标：10 个主要 sorry
  - 已完成：3 个（30%）
  - 剩余：7 个（70%）

  最近完成的证明：

  1. diagonal_mul (行104-115)
     证明：对角矩阵乘积仍为对角
     策略：利用对角矩阵定义，分析 i≠j 时的求和项
     关键步骤：
       - 展开矩阵乘法定义
       - 分类讨论 k 是否等于 i 或 j
       - 利用 hD1 或 hD2 对角性消去项

  2. GL_symmetry (行63-72)
     证明：(U * A) * (⅟A * Z) = U * Z
     策略：矩阵结合律 + 可逆矩阵性质

  3. hA_D1_Q_D2 (行172-182)
     证明：D1 * Q * D2 = A（其中 Q = D1⁻¹ * A * D2⁻¹）
     策略：代数恒等式展开

  下一步建议：

  优先级1（高难度，核心引理）：
  - h_AAt_diag (行133)：证明 A * Aᵀ 是对角的
    这是主定理的关键，需要利用 IsUncorrelated 假设
  - h_AtA_diag (行137)：类似 h_AAt_diag
    提示：(Aᵀ * A)[i,j] = Σ_k A[k,i] * A[k,j]

  优先级2（高难度，正交性）：
  - hQ_orth (行185)：证明 Q 是正交矩阵
    依赖于 h_AAt_diag 和 h_AtA_diag
    Q = D1⁻¹ * A * D2⁻¹，需验证 Qᵀ * Q = 1

  优先级3（中等难度，正定性 + 可逆性）：
  - h_D1_pos (行150)：需要证明 (A * Aᵀ)[i,i] > 0
    策略：利用 A 可逆性，说明 A * Aᵀ 正定
  - h_D2_pos (行155)：类似
  - hD1_inv (行163)：对角矩阵可逆性构造
  - hD2_inv (行164)：对角矩阵可逆性构造

  技术债务：
  - hD1_inv 和 hD2_inv 需构造 Invertible 实例
    策略：对角元素均为正，可构造逆矩阵（对角元素取倒数）
